# Out-of-Scope Ledger

Per `DMS Architecture/final.md` § 14: anything an executing agent discovers
mid-prompt that looks like a feature gap but is **not** in the State of
the Project gets recorded here and deferred. The agent **must not** build
it in the current wave.

Entry format: `| date | wave/prompt | where found | suggested fix | deferred to |`

## Pre-seeded entries (from final.md § 14.1)

| Date | Source | Item | Deferred to |
|---|---|---|---|
| 2026-04-17 | blueprint | Desktop sync client (Electron/Tauri) | post-G3 |
| 2026-04-17 | blueprint | Terraform / Ansible modules for on-prem infra | post-G2 |
| 2026-04-17 | final.md §14.1 | Collaborative live-cursor editing | declined; not in blueprint |
| 2026-04-17 | final.md §14.1 | Custom workflow authoring UI (drag-to-create) | declined; Wave 7 is read-only designer only |
| 2026-04-17 | final.md §14.1 | DocuSign-style branded envelope UI | declined; Wave 9 is PAdES-only |
| 2026-04-17 | final.md §14.1 | AI-generated document summaries auto-appended | declined; RAG is on-demand only |

## Discovered during execution

| Date | Wave/Prompt | Where found | Suggested fix | Deferred to |
|---|---|---|---|---|
| 2026-04-17 | Wave 5 / 5.1 | `RestoreVersion` emits `dms.version.restored.v1` only, not `dms.version.uploaded.v1` | Evaluate whether restored versions should re-trigger OCR/embed. Current view: no — same blob id, OCR idempotent on blob sha. | post-G1 |
| 2026-04-17 | Wave 5 / 5.1 | Grafana panel for `document_events_published_total` | Ship with Prompt 5.2's dashboard bundle | Wave 5 / 5.2 |
| 2026-04-17 | Wave 5 / 5.1 | Integration test for end-to-end outbox→NATS→consumer | Needs Prompt 5.2 stream + Wave 13.1 CI harness | Wave 13.1 |
| 2026-04-17 | Wave 5 / 5.3 | OTEL spans in intelligence service | Tracer not configured service-wide; Surya/Paddle calls are sync; span-per-page would need per-thread tracer | Wave 13.6 (observability bundle) |
| 2026-04-17 | Wave 5 / 5.3 | Per-page 90s OCR timeout (spec wanted it, task uses 30-min total instead) | Needs a watchdog on the synchronous Surya calls | Wave 13.3 (chaos) if customer docs hit the 30m cap |
| 2026-04-17 | Wave 5 / 5.3 | `gc_processed_events` cron for ocr_processed_events | Manual SQL today | Wave 8.1 (Temporal retention cron) |
| 2026-04-17 | Wave 5 / 5.3 | DLQ auto-replay job | Manual `dms-admin nats replay` only | post-G1 |
| 2026-04-17 | Wave 5 / 5.4 | `categories` taxonomy table + FK | Migration uses `category_key TEXT` slug; FK deferred until a product need exists | post-G1 |
| 2026-04-17 | Wave 5 / 5.4 | Per-tenant cap for classify / NER consumers | Only OCR has it; classify + NER are cheap | post-G1 |
| 2026-04-17 | Wave 5 / 5.4 | OTEL tracer config for intelligence service | Required for DoD § 1.4 #8; pulled out of individual prompts | Wave 13.6 |
| 2026-04-17 | Wave 5 / 5.5 | `page_number` in Qdrant payload | Needs OCR to pass page maps forward; start_char/end_char suffice for highlighting | post-G1 |
| 2026-04-17 | Wave 5 / 5.5 | `classification_top_1` in Qdrant payload | Requires DB lookup per chunk upsert; search can join on classify event | post-G1 |
| 2026-04-17 | Wave 5 / 5.5 | Per-tenant isolated Qdrant collections (`isolated-vector-store` opt-in) | Single-collection + payload filter handles pilot | post-G2 |
| 2026-04-17 | Wave 5 / 5.5 | Async embedding concurrency cap of 4 | Embedder is sync today; constant is advisory | post-G1 |
| 2026-04-17 | Wave 6 / 6.1 | `dms-admin kms rewrap --tenant <id>` online re-wrap of historical DEKs | Rotation retires alias but keeps it resolvable; post-compromise re-wrap needs streaming copy | Wave 6 follow-up |
| 2026-04-17 | Wave 6 / 6.1 | ~~Auto-allocate v1 KEK at tenant creation~~ | Wired into `billing/internal/provisioner.Provision` step 1a (2026-04-19). Mirrors `dms-admin kms create`; idempotent via ON CONFLICT. Note: backlog originally pointed at auth service, but Register creates users-into-tenants, not tenants. The actual tenant-creation site was billing's Provisioner. | **closed** |
| 2026-04-17 | Wave 6 / 6.1 | 24h CMK scheduled deletion for de-provisioned tenants | Needs control-plane workflow + Vault/AWS CMK lifecycle | Wave 12.3 |
| 2026-04-17 | Wave 6 / 6.1 | VaultKeyManager + AWSKMSKeyManager implementations | Stubs only; local HKDF is dev/on-prem default | Wave 6 follow-up |
| 2026-04-17 | Wave 6 / 6.1 | `kms_derive_total`, `kms_decrypt_errors_total` Prom metrics | LocalKeyManager doesn't yet expose a prometheus registry | Wave 13.6 |
| 2026-04-17 | Wave 6 / 6.2 | Refresh-token cookie `dms_refresh` on `/auth` path | Needs new schema + rotation endpoint; one-shot 24h sessions cover pilot | post-G1 |
| 2026-04-17 | Wave 6 / 6.2 | CSRF middleware on document/storage/search/billing services | Only auth wrapped today; frontend sets header on all mutations so exploitability low | post-G1 |
| 2026-04-17 | Wave 6 / 6.2 | Bearer-session deprecation warning log | Would flood current client logs; turn on when 30-day window starts | post-G1 |
| 2026-04-17 | Wave 6 / 6.2 | `csrf_rejections_total{reason}` Prom counter | Middleware currently writes 403 without emitting a labelled metric | Wave 13.6 |
| 2026-04-17 | Wave 7 / 7.4 | ~~Workflow service queries skip `SET LOCAL app.current_tenant` (RLS enforced only via WHERE clause)~~ | Fixed in Wave 11.1 — `services/workflow/internal/activities/tenant.go` introduces `runTenant` helper; repository + every activity now route through `WithTenantTx`. Backlog audit confirmed zero direct pool calls outside the helper (2026-04-19). | **closed** |
| 2026-04-17 | Wave 7 / 7.4 | Tasks page missing Delegate action | Needs user-picker component | Wave 10 |
| 2026-04-17 | Wave 7 / 7.4 | Task-detail drawer (full instance timeline) | New component work | Wave 10 |
| 2026-04-17 | Wave 7 / 7.4 | Real-time inbox updates | SSE / NATS bridge; polling covers pilot | post-G1 |
| 2026-04-17 | Wave 7 / 7.5 | Auto-layout (dagre/elkjs) for large workflow DAGs | Hand-tuned coords are fine for ≤20-node definitions; swap when customer exceeds | post-G1 |
| 2026-04-17 | Wave 7 / 7.5 | Live instance overlay on designer (highlight active step) | Needs instance-state lookup + per-step status color; folds into task detail drawer | Wave 10 |
| 2026-04-17 | Wave 7 / 7.5 | Playwright visual-regression for workflow graph | E2E harness ships later | Wave 13.1 |
| 2026-04-20 | Wave 7 / 7.5 | Reassign modal on pending designer nodes (calls `ReviewerReassignedSignal`) | Backend signal already wired in review.go:39. UI counterpart folds into the task-detail drawer work tracked for Wave 10 on line 50. | Wave 10 |
| 2026-04-20 | Wave 6 / 6.2 | `VerifyRecoveryCode` does not revoke remaining recovery codes or flag the user for forced MFA re-enrollment | Current `services/auth/internal/service/mfa.go:VerifyRecoveryCode` consumes only the matched code. DoD for the MFA-recovery login flow wants ALL remaining codes invalidated + a forced re-enrollment prompt on next login. Needs a `users.mfa_reenroll_required` flag + backend RevokeAllRecoveryHashes in the same tx + middleware enforcement on next authenticated request. UI flashes a warning toast today (login.tsx) until this lands. | Wave 6 follow-up |
| 2026-04-20 | Wave 17 / collab | Frontend React hook subscribing to the collaboration WS (`comment.created` / `typing.*` / `presence.*`) + TanStack Query cache invalidation on comment events | Backend brief-compliant after 2026-04-20 rewrite (zod schemas, cookie+CSRF at upgrade, policy check on room.join, new event names). No UI consumer exists — the DoD's "User B sees comment within 200ms" is only verified at wire level via `services/collaboration/test/comment-broadcast.test.js`. UI-driven e2e depends on this hook landing. | Wave 10 follow-up |
| 2026-04-20 | Wave 17 / collab | Playwright two-browser e2e for realtime comment fan-out | Depends on the frontend WS hook above; wire-level coverage exists via the Node integration test. | Wave 13.1 |
| 2026-04-20 | Wave 17 / collab | Policy-deny assertion in the collab integration test | `POLICY_SERVICE_URL` is read at module-load, so swapping it per-test requires a test seam (e.g. injecting a policy fn or reading from `process.env` on each call). Currently only the happy path is asserted; negative path is covered by inspection only. | Wave 11 (refactor during policy-service revamp) |
| 2026-04-20 | Wave 24 / mobile | EAS dev-client migration | `react-native-document-scanner-plugin` is a native module and is incompatible with Expo Go. The capture + offline-cache code landed 2026-04-20 references it; running on a device requires `eas build --profile development` once. Until then the scanner button throws at runtime and only the file-picker fallback works. | pilot kick-off |
| 2026-04-20 | Wave 24 / mobile | Real device / simulator verification of upload-queue resume + background drain | Code parses; cannot exercise `expo-background-fetch` / `@react-native-community/netinfo` / `expo-sqlite` on this host. The airplane-mode DoD test needs a real device or at minimum an emulator pass. | Wave 13.1 (mobile e2e harness) |
| 2026-04-20 | Wave ?? / connector | `connector_configs` schema drift between repository.go and 000001_initial_schema.up.sql | Repo queries `provider, config, status, error_message`; schema has `connector_type, config_encrypted, oauth_tokens_encrypted, sync_status, is_active`. The existing UpsertConnector/GetConnector will fail at runtime. Needs either a rename migration or a repo rewrite — design call. Blocks: encrypted OAuth token persistence (see next row). | Wave ?? (must precede further connector work) |
| 2026-04-20 | Wave ?? / connector | Encrypted refresh-token storage + degraded status + admin notify on refresh failure | PKCE + token-exchange flow landed 2026-04-20 (callback returns tokens but does NOT persist). Persistence + degraded transitions + `dms.connector.degraded.v1` emission all block on the schema-drift reconcile above. Also requires adding `dms.connector.>` to `pkg/events.DefaultStreams`. | Wave ?? (after schema fix) |
| 2026-04-20 | Wave ?? / helm | Image pinning by `@sha256:<digest>` instead of `:<tag>` in all service charts | All 14 service charts use `{{ imageRegistry }}/{{ svc }}:{{ imageTag }}`. Brief wants digest pinning. This is a CI workflow change: `.github/workflows/images.yml` should write digests into `values-release.yaml` on every tag build, then umbrella chart consumes that. Not a template change. | Wave 13.5 (release automation) |
| 2026-04-20 | Wave ?? / helm | Per-service `ServiceAccount` + RBAC | Templates use the default service account in the release namespace. Hardening wants one SA per service with tight RBAC (reads own configmaps/secrets only). Needs a `_serviceAccount.tpl` helper + per-service role/rolebinding. | post-G1 hardening |
| 2026-04-20 | Wave ?? / secrets | Auto-revoke of OLD secret after dual-validity window | `dms-admin secrets rotate` lands the new secret + sets `revoke_not_before`, but nothing automatically deletes the old value at that timestamp. Runbook 12 documents the manual revoke steps. Wants a Temporal cron (or k8s CronJob) that polls system_secrets + Redis for expired previous_values and executes the revoke. | Wave 13.5 (release automation) |
| 2026-04-20 | Wave ?? / secrets | Dedicated `ROTATION_EVENTS` JetStream stream binding | `dms.rotation.>` currently lands on JetStream's catchall; `pkg/events.DefaultStreams` has no explicit binding. Low urgency — rotation volume is operator-triggered, not application-generated — but a dedicated subject set would make the `rotation_id` dedupe guarantees clearer. | Wave 13.6 (observability bundle) |
| 2026-04-17 | Wave 8 / 8.1 | Two-person dispose-approval workflow (archived → disposed + ciphertext shred) | Interactive workflow separate from retention cron; cron emits `dispose_candidate.v1` only | Wave 8 follow-up |
| 2026-04-17 | Wave 8 / 8.1 | Retention-policy rules engine populating `documents.retention_until` on create | `retention_policies` table exists but isn't consulted at document create time | Wave 8 follow-up |
| 2026-04-17 | Wave 8 / 8.1 | Admin UI for retention-policy CRUD | No endpoints yet; policies edited via direct SQL | Wave 10 |
| 2026-04-17 | Wave 8 / 8.1 | `retention_*_total` Prom counters | Activity instrumentation needed | Wave 13.6 |
| 2026-04-17 | Wave 8 / 8.2 | OPA `role=compliance_officer` gate on hold create/release | Handler reads headers only; needs policy.Check call or middleware | Wave 11 |
| 2026-04-17 | Wave 8 / 8.2 | Redaction endpoint + hold gate on it | Endpoint doesn't exist yet | Wave 12 |
| 2026-04-17 | Wave 8 / 8.2 | Proper create-hold UI (doc multi-select) + release modal | Placeholder uses window.prompt today | Wave 10 |
| 2026-04-17 | Wave 8 / 8.2 | ~~Remove dead code in compliance/retention.go (CreateLegalHold/ReleaseLegalHold/RetentionEnforcer)~~ | retention.go was already deleted; stale referencing comment in holds.go cleaned up 2026-04-19. | **closed** |
| 2026-04-17 | Wave 8 / 8.2 | Hold-extension / expiry column | Schema has no expiry; spec's "extend" clause is vacuous until added | Wave 8 follow-up |
| 2026-04-17 | Wave 8 / 8.2 | `legal_holds_applied_total` / `legal_holds_released_total` Prom counters | Wave 13.6 |
| 2026-04-17 | Wave 8 / 8.3 | ZIP packaging + signed-URL export for DSR | Needs DSR bucket + storage-service presigned-upload path; workflow returns counts only today | Wave 11 (storage integration) |
| 2026-04-17 | Wave 8 / 8.3 | Cross-service erase activities (qdrant, notifications, search) | Each service registers its own activity with workflow | Wave 11 |
| 2026-04-17 | Wave 8 / 8.3 | DSR verification-token email round-trip | Honor-system today (ADR 0024 §6); needs notification service transactional email | Wave 11 |
| 2026-04-17 | Wave 8 / 8.3 | `privacy_ledger` 7-year retention sweep | Operator SoP today; cron lands later | Wave 12 |
| 2026-04-17 | Wave 8 / 8.3 | "DSR running > 25 days" alert (approaching 30-day SLA) | Prom alert + dashboard | Wave 13.6 |
| 2026-04-17 | Wave 8 / 8.3 | `dsr_requests_total{type,status}` / `dsr_hold_blocks_total` Prom counters | Wave 13.6 |
| 2026-04-17 | Wave 8 / 8.4 | Physical blob move + re-encrypt under region-local KEK | Needs storage-service cross-region copy activity; today workflow flips `region_pin` + emits event only | Wave 12 |
| 2026-04-17 | Wave 8 / 8.4 | Per-tenant default region override admin endpoint | `organizations.region_pin` exists but no CRUD; control-plane work | Wave 12.3 |
| 2026-04-17 | Wave 8 / 8.4 | Cancel / pause running migration from admin UI | `temporal workflow cancel` works today | post-G1 |
| 2026-04-17 | Wave 8 / 8.4 | Multi-region KEK aliases (separate master per region) | Wave 6.1 single-master derivation; prod needs Vault/AWS KMS per region | Wave 12 |
| 2026-04-17 | Wave 8 / 8.4 | `residency_migrations_total{target}` / `residency_doc_moves_total` Prom counters | Wave 13.6 |
| 2026-04-17 | Wave 9 / 9.1 | DSS-sidecar performance benchmark validation | ADR claims 8-10 PAdES-B-LT/sec/core; needs harness | Wave 13.3 |
| 2026-04-17 | Wave 9 / 9.1 | Threat model for Go↔Java sidecar boundary | Secrets per-request, not at rest; formal TM later | Wave 13.4 |
| 2026-04-17 | Wave 9 / 9.2 | Java DSS sidecar (Gradle project, Dockerfile, gRPC Sign/Verify) | Follow-up prompt; signer interface + mock land now | Wave 9.2b |
| 2026-04-17 | Wave 9 / 9.2 | Temporal workflow wiring to signer.Signer | SignatureWorkflow still the Wave 7.1 stub | Wave 9.2b |
| 2026-04-17 | Wave 9 / 9.2 | /signatures/envelopes route aliases with fields[] body | Existing /requests shape covers the same data | Wave 9.2b |
| 2026-04-17 | Wave 9 / 9.2 | Admin in-flight envelopes page | Reuses existing APIs | Wave 9.2b |
| 2026-04-17 | Wave 9 / 9.2 | Adobe Reader validation harness in CI (pdf-signature-validator) | DoD depends on real sidecar | Wave 9.2b |
| 2026-04-17 | Wave 9 / 9.2 | Vault / AWS KMS / PKCS#11 signer backends | LocalKeyManager works in dev; prod backends need adapters | Wave 12 |
| 2026-04-17 | Wave 9 / 9.2 | `signature_*` Prom counters (envelopes/sign duration/TSA errors/KMS errors/LTV missing) | Runbook documents planned metrics | Wave 13.6 |
| 2026-04-17 | Wave 10 | User-picker combo for group member add (currently raw UUID input) | UI polish | Wave 10 follow-up |
| 2026-04-17 | Wave 10 | Bulk add/remove group members | Easy follow-up | Wave 10 follow-up |
| 2026-04-17 | Wave 10 | Go handler-level tests for /api/v1/admin/groups | Package lacks test harness | Wave 10 follow-up |
| 2026-04-17 | Wave 10 | Tag rename | Needs `documents.tags` array scrub across docs | Wave 10 follow-up |
| 2026-04-17 | Wave 10 | Tag bulk delete / color edit | Follow-up | Wave 10 follow-up |
| 2026-04-17 | Wave 10 | Custom-hex color input on Tags page (currently 12 presets) | UI polish | Wave 10 follow-up |
| 2026-04-18 | Wave 10 | Live rego introspection for permission matrix | Curated Go mirror today; CI drift guard (`scripts/check-policy-matrix-sync.sh`) planned | Wave 11 |
| 2026-04-18 | Wave 10 | Per-tenant permission matrix overrides | Global matrix today; needs tenant-scoped layer when custom rego ships | Wave 11 |
| 2026-04-18 | Wave 10 | "Why can Alice edit X?" explain endpoint | OPA trace mode exists but no HTTP surface | Wave 11 |
| 2026-04-17 | Wave 10 | SSO wizard (SAML/OIDC metadata upload + test assertion) | Multi-step flow | Wave 10 follow-up |
| 2026-04-18 | Wave 10 | Retention policies: last-accessed / custom trigger | Schema uses created_at only | Wave 10 follow-up |
| 2026-04-18 | Wave 10 | Retention policies: "which docs match" preview | Needs filter-evaluation endpoint | Wave 10 follow-up |
| 2026-04-18 | Wave 10 | `retention_policies_*_total` Prom counters | Wave 13.6 |
| 2026-04-17 | Wave 10 | Metadata schema editor (JSON Schema, monaco) | UI work | Wave 10 follow-up |
| 2026-04-17 | Wave 10 | Tags admin (tenant-level tag CRUD) | Per-doc tagging exists | Wave 10 follow-up |
| 2026-04-17 | Wave 10 | `dms.sharelink.revoked.v1` outbox event on individual + bulk revoke | Audit relies on `audit_events` today via OPA requirePermission | Wave 10 follow-up |
| 2026-04-17 | Wave 10 | Share-link filter (title/creator/expires-before) + CSV export | UI polish | Wave 10 follow-up |
| 2026-04-17 | Wave 10 | `share_links_revoked_total` / `share_links_revoke_all_total` Prom counters | Wave 13.6 |
| 2026-04-17 | Wave 10 | Share-link admin (list/revoke/revoke-all per doc) | Share-link API exists | Wave 10 follow-up |
| 2026-04-17 | Wave 10 | Webhook pause/resume toggle, delivery-detail modal | Follow-up to webhooks page | Wave 10 follow-up |
| 2026-04-17 | Wave 10 | `webhook_rotations_total` / `webhook_redeliveries_total` Prom counters | Wave 13.6 |
| 2026-04-20 | Wave 20 / control-plane | Temporal workflow wrapper for provisioning | Synchronous procedural `provisioner.Provision` already satisfies the "live in 3 min" DoD. Brief asks for it as a Temporal workflow; would require wrapping the existing logic as an activity + new workflow definition + kicking from the HTTP handler. No functional change today — deferred until replay/resume on provisioning failure is a real ops need. | post-G1 |
| 2026-04-20 | Wave 20 / control-plane | Real KMS CMK crypto-shred on hard-dispose | `HardDisposeTenant` activity retires `tenant_keks` rows + marks `organizations.disposed_at` but does NOT call the KMS adapter's delete. AWS KMS `ScheduleKeyDeletion` (7-day min) + Vault Transit force-delete are adapter-specific; LocalKeyManager has no shred concept. | Wave ?? (after KMS adapter work) |
| 2026-04-20 | Wave 20 / control-plane | Cross-service purge activities in `HardDisposeTenant` | Purging OpenSearch indexes, Qdrant collections, and S3 objects at tenant level is currently a no-op in the activity. The retired KEK already makes wrapped blobs undecryptable, but disk/index rows linger until purge. Share the DSR erase activities (`PurgeSubjectFromSearch`, `PurgeSubjectFromVectors`) scaled from subject → tenant. | Wave 12 follow-up |
| 2026-04-20 | Wave 20 / control-plane | Payment-failure grace-period → suspend sweeper | Runbook 13 documents the nightly sweep that flips `past_due` tenants to `suspended` after 14 days. The sweeper itself isn't implemented; manual ops today. | post-G1 |
| 2026-04-20 | Wave 20 / control-plane | Starter workflow wiring from `POST /deprovision` | Backend endpoint writes soft-delete rows + schedules via DB column. The DeprovisionWorkflow itself is registered on the worker but no code path calls `client.ExecuteWorkflow(DeprovisionWorkflow, ...)`. A nightly sweeper on `dispose_scheduled_at` would be simpler than a per-tenant workflow; design call deferred. | post-G1 |
| 2026-04-20 | Wave 12.5 / schema | Per-service migrations split | Document service's `000001_initial_schema.up.sql` has **49 CREATE TABLE statements** covering schema owned by auth/policy/storage/workflow/notification/signature/billing/connector/audit. Brief wants each service to own its tables under `services/<name>/migrations/`. Execution plan: (1) add a schema-diff harness (`migra` or `pg_dump --schema-only \| diff`) under `scripts/` so the "identical diff" DoD is verifiable; (2) set up empty `migrations/` dirs per service; (3) per-service extract PR with golden diff attached — one PR per service so each can be reviewed in isolation; (4) add `dms-admin db migrate --service <name>` + `--all` with topological ordering (shared `pkg/schema/core` runs first, then services with no cross-service FKs, then the rest); (5) after the last service extracts, delete the mega-migration. Cross-service FK ordering, trigger-function `update_updated_at_column` ownership, and RLS policy creation order all need a running DB to verify — blind one-shot execution not recommended. | Wave 12.5 (multi-PR) |
| 2026-04-20 | Wave 12.4 / billing | Reshape `usage_records` to long-format | Brief 12.4 asked for `(tenant_id, metric_key, period_start, period_end, quantity, unit, aggregated_at, reported_at)`. Current shape is wide-format (one column per metric, stamped by migration 000010 to match the Go code that already INSERTs that shape). The wide form works with the cron + dashboard + Stripe Meter push; long-form would be a 3rd rewrite, blocked by 12.5's "no column change" constraint. Defer to a joint 12.4 + 12.5 follow-up where the split happens first, then the reshape. | post-G1 |
| 2026-04-20 | Wave 13.1 / CI | Add qdrant + temporal-dev + clamav-stub service containers to the integration-tests job | `.github/workflows/ci.yml:114` already boots postgres/redis/nats/minio/opensearch. Missing containers are cheap additive YAML (~20 lines). Currently no test exercises qdrant/temporal/clamav in CI, so absence isn't blocking — add when a test that needs them lands. | post-G1 |
| 2026-04-20 | Wave 13.1 / CI | Golden fixture corpus (50 PDF / 20 DOCX / 10 emails / 5 images) | `tests/integration/` has a README but no fixtures. Matches the same licensing/sourcing blocker we hit for the classify golden (2026-04-19 backlog entry) — arbitrary PDFs can't be committed; requires operator decision on synthetic-vs-public-domain-vs-licensed sourcing. | pilot kick-off |
| 2026-04-20 | Wave 13.1 / CI | 30-second smoke workflow on every branch push | Brief wants a smoke suite separate from the `integration-tests` job. Would be a new `.github/workflows/smoke.yml` running unit tests + 1-2 fast sanity integration tests on `push:` (not just `pull_request:`). ~30 lines YAML. Low urgency — unit tests already run on PR. | post-G1 |
| 2026-04-20 | Wave 13.1 / CI | 12-min p95 budget + fail-if-exceeded guard | Current `timeout: 15m` is a hard-fail ceiling, not a soft budget. Brief wants a separate signal for "slower than expected" that fails CI even when tests pass. Needs a p95-over-last-N-runs tracker — GH Actions can't do this natively, needs either Datadog CI Visibility or a lightweight workflow that queries the Actions API. | Wave 13.6 (observability bundle) |
| 2026-04-20 | Wave 13.1 / CI | CI dashboard tile | Ops-tool config (Datadog/Grafana scraping GH Actions metrics) rather than in-repo YAML. Track with operator ops board. | post-G1 |
| 2026-04-20 | Wave 13.2 / perf | Execute k6 scenarios against staging + populate `docs/performance/baseline-2026-04-20.md` | Runbook 14 documents the full procedure. Scenarios exist, runner exists, baseline stub committed with every SLO marked `TBD`. Staging cluster + pprof access is operator-side. DoD is "every missed SLO has a tracked fix PR" — need real measurements first. | operator ops (needs cluster access) |
| 2026-04-20 | Wave 13.3 / chaos | Execute 7 chaos scenarios + populate `docs/chaos/drill-2026-04-20.md` | Runbook 15 documents the quarterly drill procedure. All 7 scenario markdowns exist under `docs/chaos/scenarios/`. Drill stub committed with every verdict `TBD`. Needs staging cluster with chaos-mesh installed + background k6 load during the run. Schedule via ops calendar (first Tuesday of each quarter, 14:00 UTC per runbook). DoD "every scenario has a PASS verdict or a tracked fix" depends on the run happening. | operator ops (needs chaos-mesh cluster) |
| 2026-04-20 | Wave 13.4 / web | Full share-link + API key create/rotate journeys | 07/08 specs shipped as scoped-smoke (page loads + primary action visible). Full journeys need the doc-detail drawer's share-link UI audited (07) and an end-to-end create→rotate→revoke flow on api-keys (08). Both need a run against an actual UI to verify selectors. | Wave 10 follow-up |
| 2026-04-20 | Wave 13.4 / web | Ratchet coverage threshold from 20% to 50% | `web/vite.config.ts` thresholds set defensively to 20% so the PR lands without a CI-break. Brief targets 50% by end-of-wave. Approach: after `npm run test:ci` lands in CI, read the actual % from the coverage artifact and raise the thresholds in 10% steps as tests accrue. | Wave 13.4 follow-up |
| 2026-04-20 | Wave 13.4 / web | Initial visual-regression snapshot capture | `visual-regression.spec.ts` runs 3 snapshots (login/dashboard/tasks) but the baseline PNGs must be captured by running `npx playwright test visual-regression.spec.ts --update-snapshots` and committing the `__screenshots__` folder. Can only be done on a Linux runner matching CI's rendering (macOS/Windows snapshots diff on subpixel AA). | operator kick-off |
| 2026-04-20 | Wave 13.5 / mutation | Publish baseline mutation scores + scorecard dashboard tile | CI job + script exist (`scripts/mutesting/run.sh`, 70% threshold, label-gated). Missing: (1) a committed `docs/performance/mutation-baseline-YYYY-MM-DD.md` recording the initial per-package % once the job runs; (2) a Grafana tile that ingests the score (needs either a Prometheus gauge emitted from the CI job or a scrape of the GH artifact). Both need the job to run once against a real cluster/runner, which is operator kick-off. | operator kick-off |
| 2026-04-20 | Wave 14.2 / openapi | Shrink the drift allow-list from 39 paths → 0 | `scripts/openapi/drift-allowlist.txt` committed 2026-04-20 to grandfather existing undocumented routes so the hard-fail drift-check gate can land without breaking CI. Each entry is work: read the handler, write the request/response schemas, add path + verb to `docs/api/openapi.yaml`, remove the line from the allow-list. Owner per-service — service engineers should write specs for their own routes rather than a batch blind sweep. | per-service, ongoing |
| 2026-04-20 | Wave 14.2 / openapi | Fix `scripts/openapi/check-drift.sh` path-prefix handling | Current script produces noisy output: paths like `/api/v1/{param}` appear undocumented because the greps capture route-relative paths without knowing each service's mounted prefix. The allow-list tolerates the noise today; a proper fix would grep each service's `cmd/server/main.go` mount point + prefix the extracted paths accordingly. | Wave 14.2 follow-up |
| 2026-04-20 | Wave 14.2 / openapi | Run SDK codegen + commit `clients/{go,ts,python}/` | Makefile targets `sdk-gen-{go,ts,py}` wired 2026-04-20 but haven't been run. Running today would produce SDKs that miss ~40 grandfathered routes. Schedule after the allow-list is < 10 entries. | after allow-list shrinks |
| 2026-04-20 | Wave 14.2 / openapi | In-cluster Swagger UI at `/docs` | GH Pages deploy at `openapi-pages.yml` serves Swagger UI externally today. In-cluster route requires a Kong route entry + a swagger-ui-dist container (small nginx pod). Brief wanted `/docs` reachable on the gateway; external GH Pages covers the DoD spirit (renders + live examples) but not the literal path. | post-G1 |
| 2026-04-20 | Wave 13.7 / DR | Execute first DR rehearsal + produce signed report | `docs/runbooks/dr/` directory + per-tenant-restore + tabletop-template + rehearsals/README committed 2026-04-20. Stub at `rehearsals/2026-04-20-stub.md` reserves the path. First runnable drill = monthly tabletop (1 h, zero cluster touch, just needs IC + peer on a call). Postgres PITR drill is quarterly, needs staging. | operator ops (IC kick-off) |
| 2026-04-20 | Wave 13.7 / DR | `dms-admin restore-tenant --tenant <id> --as-of <ts>` orchestration | Per-tenant-restore runbook documents a 7-step manual sequence across psql + mc + OpenSearch + Qdrant. Automating it as a CLI with per-step rollback is the next leverage point. ~300 LOC; requires backup-snapshot schema to be stable first. | post-G1 |
| 2026-04-20 | Wave 14.4 / security | Sign the threat model (3 roles) | `docs/security/threat-model.md` Approvals table has TBD rows for Security lead / Platform eng lead / CTO. DoD gate for pilot launch — requires reviewer to walk the full doc end-to-end. | pilot launch gate |
| 2026-04-20 | Wave 14.4 / security | External pentest engagement + findings remediation | `docs/security/threat-model.md` Pentest engagement plan names scope + SLA + retest + disclosure. Vendor selection + contract + scheduling is operator ops. Findings land as `docs/security/pentest-findings/PT-YYYY-NNN.md` using the existing template. Pilot launch gated on all HIGH+ resolved. | pilot launch gate |
| 2026-04-20 | Wave 14.5 / airgap | Execute `build-ai-pack.sh` + produce signed `vaultdms-ai-pack-*.tar.gz` | Script committed 2026-04-20. Downloads ~14 GB of model weights from HuggingFace (Surya OCR + embed + Llama 3.1 8B). Requires a runner with that much disk + outbound access. Cosign sign-blob the resulting tarball. Release automation (`.github/workflows/release.yml`) should pick this up when the pack is ready — today's release workflow covers the core bundle only. | release engineering |
| 2026-04-20 | Wave 14.5 / airgap | End-to-end 30-min install verification on a clean air-gapped VM | `dms-installer check/load/install/verify` subcommands committed 2026-04-20 as a single static binary. Can't validate the 30-min-to-login DoD without a real airgap VM. First dry-run: `dms-installer check` on an internal staging image, then `load --dry-run`, then a non-dry-run install in a sandbox cluster. | operator ops |
| 2026-04-20 | Wave 14.5 / airgap | Offline docs site bundled into the tarball | Brief names an offline docs site. Existing `openapi-pages.yml` produces a GH Pages site; we'd either (a) mirror that HTML into the airgap tarball, or (b) build a mkdocs site from `docs/` and bundle it. Neither is done. | post-G1 |
| 2026-04-20 | Wave 14.6 / release | Upgrade SLSA provenance from "build level 2" to level 3 | `release.yml` produces a slsaprovenance.v1 attestation via `cosign attest --type slsaprovenance` inside the per-service matrix job. That's level 2 — the builder is a standard GitHub runner, not the hardened reusable workflow. Level 3 requires `slsa-framework/slsa-github-generator/generator_container_slsa3.yml` per image, which needs an intermediate "collect-digests" job because GH Actions matrix outputs can't directly feed a reusable workflow's `uses:` inputs. Pattern: matrix image job → per-service digest artifacts → aggregate-job emits JSON map → matrix provenance job consumes it. | post-G1 (supply-chain hardening) |
| 2026-04-20 | Wave 14.6 / release | First end-to-end `release-please` run | Config + workflow committed 2026-04-20. First run against `main` will open 17 release PRs (one per package per the config). Ops work: merge each, validate per-service CHANGELOG.md generation, verify tags like `storage-v0.1.1` trigger the existing release workflow correctly. | release engineering first exec |
| 2026-04-20 | Wave 15 / Phase 3 | Execute `scripts/smoke-e2e.sh` + populate `SMOKE_RUN_2026-04-20.md` | Orchestrator committed 2026-04-20. Requires Docker + Node + k6 + Chrome. Cold-boot-to-login target <15 min per W14.1 DoD. Cross-tenant isolation at §3.5 is a HARD-STOP P0. | operator workstation |
| 2026-04-20 | Wave 15 / full readiness matrix | Complete the sampled sections of `ENTERPRISE_READINESS_2026-04-20.md` | Report covers all 2.1-2.8 sections with honest coverage statements. Sampled: full 96-route attribute matrix (2.1), 43-path connectivity CSV (2.3), all 35 frontend routes against every UI quality criterion (2.4), CORS + span-attribute deep dives (2.5, 2.6). Each gap needs a service-owner walkthrough + attached screenshots. | per-service owners, pre-G2 |
| 2026-04-20 | Wave 15 / F1 | Idempotency-Key header support on mutating endpoints | Backend route audit (2.1) found no routes honour `Idempotency-Key`. Only Stripe webhook dedups (`stripe_events`). Client retries during transient 5xx produce duplicates. Implement a middleware that hashes (tenant, method, path, Idempotency-Key) → cached response for N minutes. | P1, pre-G2 |
| 2026-04-20 | Wave 15 / F6 | Per-route React error boundaries | Root boundary at `__root.tsx` catches inner-route crashes into the full-app fallback. Add per-route `<ErrorBoundary>` in the authenticated layout so one page's crash is contained. | P1 UI polish |
| 2026-04-20 | Wave 15 / F12 | Remove `unsafe-inline` from CSP `style-src` | `pkg/gateway/waf.go:57` has `style-src 'self' 'unsafe-inline'`. Brief flags as prod blocker. Root cause: shadcn + Tailwind JIT inline styles. Fixes: (1) hash-based CSP for known blocks, (2) refactor components to class-only. Non-trivial. | P1, pre-G2 |
| 2026-04-20 | Wave 15 / F10 | i18n wiring or removal | `react-intl` is a dependency but `IntlProvider` is never mounted and no message catalog exists. Either wire it properly (adds routing + catalog management work) or remove the dep. | P2 |

## Wave 15 scope flip (2026-04-21)

The four items below move from "deferred" to **approved scope** under Wave 15.
Execution order is strict per the Wave 15 brief: 15.3 → 15.2 → 15.1 → 15.4,
one PR per sub-wave. Progress is tracked in `docs/reports/WAVE_15_PROGRESS.md`.

| Date | Sub-wave | Item | Rationale |
|---|---|---|---|
| 2026-04-21 | 15.3 | Force-change-password on first login / admin reset / N-day expiry | Enterprise procurement gate (SOC2 CC6.1) — customers block purchase until rotation is enforced, not just requested. |
| 2026-04-21 | 15.2 | Geofencing / IP allowlists per tenant / workspace / document | Parity with Raabyt/DMS prototype + ITAR / EU-only commitments already promised in signed MSAs. |
| 2026-04-21 | 15.1 | Acknowledgement campaigns (policy attestations) | Top-3 feature ask from pilot compliance officers; no third-party product in our bundle provides tamper-evident attestations. |
| 2026-04-21 | 15.4 | Saved signature profiles | UX friction on high-volume PAdES signers; Wave 9 crypto is stable — additive only, no PAdES changes. |

## Rules

1. If a prompt's scope balloons past its defined deliverables, stop and
   log here rather than growing the PR.
2. If a feature is needed to make another feature work, log it but still
   finish the current prompt with a stub; do not re-scope.
3. Re-check this ledger at the start of every wave to see if anything
   deferred is now in scope.
