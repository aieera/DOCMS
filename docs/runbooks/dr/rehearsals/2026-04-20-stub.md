# DR rehearsal — 2026-04-20 (STUB)

**Status**: stub — rehearsal NOT executed. This file exists to
reserve the path for the next scheduled drill (per the cadence in
[../README.md](../README.md)).

The brief (§13.7) asked for an executed drill with a signed report.
Executing requires a staging cluster copy with real backups in place
+ an on-call roster available to run the exercise + 4 h of
coordinated time. All operator-side; cannot be stubbed from code.

Replace this file (or create a new `YYYY-MM-DD-tabletop.md`) after
the first drill runs.

## What a populated report would contain

See [README.md](README.md#contents-per-report). Briefly:

- Date / type / IC / participants
- Scenario ran (from [tabletop-template.md](../tabletop-template.md))
- Timeline with timestamps
- RTO achieved vs target
- RPO observed
- Gaps found (runbook, tooling, comms)
- Sign-off

## Until the first drill

Tabletop exercises are zero-cluster-touch and can run as soon as an
on-call engineer and a peer are on the same call for 1 hour. They
produce immediately-useful runbook feedback with no infrastructure
dependencies — recommended as the first entry under this directory.
