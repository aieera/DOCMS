# Performance baseline — 2026-04-20

**Status: stub — pending staging execution.** See [runbook 14](../runbooks/14-performance-baseline.md) for how to populate this. Every row below is `TBD` until a real run writes measurements.

The brief (§13.2) scoped the execution of k6 scenarios 01-08 against a staging cluster sized to pilot target. Scenarios exist; k6 runner exists; staging cluster + runner access is operator-side. This doc is created empty so the structure survives rebases and the eventual runner doesn't have to reinvent the layout.

## Run metadata

- **Date:** 2026-04-20 (stub); replace with actual run date when populated.
- **Commit:** TBD
- **Cluster:** TBD (staging target per brief: 3× stateless core services, 3-node Postgres/OpenSearch/NATS, single-node Qdrant)
- **Seed data:** 10 tenants × 500 documents via `./scripts/seed/seed.sh --tenants 10 --docs-per-tenant 500`. **Required** — missing seed makes every search-side measurement meaningless.
- **Runner:** TBD
- **Wall clock:** TBD (estimated ~75 min per runbook 14)

## Summary

| SLO | Target | Observed | Pass? | Fix PR |
|---|---|---|---|---|
| Upload initiate p99 | < 200 ms | TBD | ☐ | |
| Search p99 (mixed lex+sem, 70/30) | < 300 ms | TBD | ☐ | |
| OCR end-to-end p95 | < 30 s | TBD | ☐ | |
| Permission check p99 | < 5 ms | TBD | ☐ | |
| Auth login p99 | < 200 ms | TBD | ☐ | |
| WS concurrent per pod | ≥ 1 000 | TBD | ☐ | |
| Cross-tenant leaks | 0 | TBD | ☐ | |

**DoD reminder**: every ☐ row must carry a linked fix PR before this doc merges. A ☐ with a PR is acceptable; a ☐ without one blocks merge.

## Per-scenario details

### 01 — document CRUD (`tests/load/scenarios/01-document-crud.js`)

- Peak RPS: TBD
- p50 / p95 / p99: TBD / TBD / TBD
- Slow endpoints (top 5): TBD
- Bottleneck hypothesis: TBD (candidates: policy check per op, outbox publisher backpressure)
- CPU flamegraph: `docs/performance/profiles/TBD-document-cpu.svg`
- Grafana: TBD

### 02 — search (2000 q/s, 70% lexical / 30% semantic)

- Peak RPS: TBD
- p50 / p95 / p99: TBD / TBD / TBD
- Lexical p99 vs semantic p99: TBD
- OpenSearch warmup effect: TBD
- Qdrant query time contribution: TBD
- CPU flamegraph: `docs/performance/profiles/TBD-search-cpu.svg`

### 03 — upload (500/min steady, 2000/min burst 5 min)

- Steady-phase p99: TBD
- Burst-phase p99: TBD
- ClamAV scan latency (in-path): TBD
- MinIO PUT latency: TBD
- Envelope encrypt CPU: TBD

### 04 — OCR pipeline (end-to-end)

- p95 wall-clock from `dms.version.uploaded.v1` → `dms.ocr.completed.v1`: TBD
- Per-page throughput: TBD
- Surya vs Paddle engine selection rate: TBD

### 05 — WebSocket (shared doc room)

- Joins/s: TBD
- Broadcast fan-out RTT p95: TBD

### 06 — mixed realistic

- Combined RPS: TBD
- Any SLO regression vs isolated scenarios: TBD

### 07 — cross-tenant isolation (10 tenants)

- `isolation_violations` counter: **MUST be 0**
- If non-zero: **P0 incident**; file under [docs/security/](../security/) not here.

### 08 — collaboration WS (1000 conns/pod)

- Connections sustained per pod: TBD
- Broadcast RTT p95: TBD
- Handshake p95 (cookie + CSRF): TBD

## Cost per 1 000 operations

Derived from staging cloud billing export for the runtime window, divided by `http_reqs` total. See runbook §"Cost per 1k operations".

- Upload: $TBD / 1k
- Search: $TBD / 1k
- OCR: $TBD / 1k doc (dominated by compute on the intelligence worker)

## Bottlenecks + proposed fixes

Populate from the pprof flamegraphs after the run. Empty stub sections until then:

- **Service:** TBD
  - Observation: TBD
  - Fix PR: #TBD

## Change since last baseline

No prior baseline exists. Subsequent runs should add a "Change since 2026-04-20" subsection per SLO summarising direction of movement.
