# VaultDMS — local development infrastructure.
# Bring up with `make docker-up`. Services (Go binaries) run on the host during
# development; this compose file provides only their dependencies.

name: vaultdms-dev

# ----- Shared anchors for Go services (§3.2 / A2 Sub-task B) -----
# Docker Compose permits free-form top-level keys prefixed with `x-`.
# Anchored here so they can be merged into each Go-service block via *.
x-go-env: &go-env
  # §3.1 / B2.2 — shared secret; pkg/middleware.RequireGatewaySignature
  # rejects any request that doesn't carry a matching X-Gateway-Signature.
  # Must be identical to the gateway's VAULTDMS_GATEWAY_SECRET.
  VAULTDMS_GATEWAY_SECRET: "${VAULTDMS_GATEWAY_SECRET:-dev-only-gateway-secret-rotate-in-prod}"
  VAULTDMS_DATABASE_URL: "postgres://vaultdms:devpassword@postgres:5432/vaultdms?sslmode=disable"
  VAULTDMS_REDIS_URL:    "redis:6379"
  VAULTDMS_NATS_URL:     "nats://nats:4222"
  VAULTDMS_HEALTH_PORT:  "8081"
  VAULTDMS_HTTP_PORT:    "8080"
  VAULTDMS_GRPC_PORT:    "9090"
  VAULTDMS_S3_ENDPOINT:  "minio:9000"
  VAULTDMS_S3_ACCESS_KEY: "minioadmin"
  VAULTDMS_S3_SECRET_KEY: "minioadmin"
  VAULTDMS_OPENSEARCH_URL: "http://opensearch:9200"
  VAULTDMS_QDRANT_URL:   "http://qdrant:6333"
  VAULTDMS_TEMPORAL_ADDR: "temporal:7233"
  # §7.1 / D6 part 2 — search service's dense-vector path. Both must
  # be set for hybrid/semantic mode to activate; otherwise Search()
  # degrades to lexical and logs a notice.
  VAULTDMS_INTELLIGENCE_EMBED_URL: "http://intelligence:8080/internal/v1/embed-query"
  VAULTDMS_QDRANT_COLLECTION: "vaultdms_chunks"
  # §10.3 / E6 — OnlyOffice integration
  VAULTDMS_ONLYOFFICE_URL: "http://onlyoffice"  # in-cluster DNS
  VAULTDMS_ONLYOFFICE_PUBLIC_URL: "http://localhost:8195"  # what the browser sees
  VAULTDMS_ONLYOFFICE_JWT: "${VAULTDMS_ONLYOFFICE_JWT:-dev-only-onlyoffice-jwt-rotate-in-prod}"
  POLICY_SERVICE_ADDR:   "policy:9090"
  STORAGE_SERVICE_ADDR:  "storage:9090"
  VAULTDMS_ENV:          "development"
  # Consumed by the workflow service's platform metrics proxy
  # (/api/v1/platform/metrics/query). Harmless noise for other services.
  VAULTDMS_PROMETHEUS_URL: "http://prometheus:9090"

x-go-depends: &go-depends
  postgres: { condition: service_healthy }
  redis:    { condition: service_healthy }
  nats:     { condition: service_healthy }

x-go-healthcheck: &go-healthcheck
  test: ["CMD", "wget", "-q", "--spider", "http://localhost:8081/healthz"]
  interval: 10s
  timeout: 3s
  retries: 5
  start_period: 30s

services:
  postgres:
    image: postgres:16-alpine
    container_name: vaultdms-postgres
    environment:
      POSTGRES_DB: vaultdms
      POSTGRES_USER: vaultdms
      POSTGRES_PASSWORD: devpassword
    ports: ["15432:5432"]
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./scripts/init-db.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vaultdms"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: vaultdms-redis
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    ports: ["6379:6379"]
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  opensearch:
    image: opensearchproject/opensearch:2.12.0
    container_name: vaultdms-opensearch
    environment:
      - discovery.type=single-node
      - plugins.security.disabled=true
      - OPENSEARCH_INITIAL_ADMIN_PASSWORD=DevOnly!Passw0rd
      - OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m
      - bootstrap.memory_lock=true
    ulimits:
      memlock: { soft: -1, hard: -1 }
      nofile: { soft: 65536, hard: 65536 }
    ports: ["9200:9200", "9600:9600"]
    volumes:
      - opensearchdata:/usr/share/opensearch/data
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9200/_cluster/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  minio:
    image: minio/minio:RELEASE.2024-02-17T01-15-57Z
    container_name: vaultdms-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports: ["9000:9000", "9001:9001"]
    volumes:
      - miniodata:/data
    # MinIO image ships with bash + mc but not curl/wget. Use bash's
    # /dev/tcp pseudo-file to probe the S3 port — same semantics as a curl
    # against /minio/health/live but with zero extra binaries.
    healthcheck:
      test: ["CMD", "bash", "-c", "exec 3<>/dev/tcp/localhost/9000"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  minio-init:
    image: minio/mc:latest
    container_name: vaultdms-minio-init
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 minioadmin minioadmin;
      mc mb --ignore-existing local/dms-us-east-1-hot;
      mc mb --ignore-existing local/dms-us-east-1-warm;
      mc mb --ignore-existing local/dms-us-east-1-previews;
      mc mb --ignore-existing local/dms-quarantine;
      mc mb --ignore-existing local/health-check;
      echo 'bucket provisioning complete';
      "
    restart: "no"

  nats:
    image: nats:2.10-alpine
    container_name: vaultdms-nats
    command: -js -sd /data -m 8222
    ports: ["4222:4222", "8222:8222"]
    volumes:
      - natsdata:/data
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8222/healthz || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  # Prometheus scrapes each service's /metrics on its health port.
  # Consumed by the platform admin UI via workflow's
  # /api/v1/platform/metrics/query PromQL proxy; the browser never
  # talks to Prometheus directly because Prom has no auth of its own.
  #
  # Config + rules are baked into the image via the volume mount.
  # In production this is the cluster Prometheus operator, not this
  # container.
  prometheus:
    image: prom/prometheus:v2.51.0
    container_name: vaultdms-prometheus
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.retention.time=7d
      - --web.enable-lifecycle
    ports: ["9090:9090"]
    volumes:
      - ./ops/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./ops/prometheus/rules:/etc/prometheus/rules:ro
      - prometheusdata:/prometheus
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:9090/-/ready || exit 1"]
      interval: 15s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  # §10.3 / E6 — OnlyOffice Document Server for DOCX/XLSX/PPTX
  # co-editing. JWT-enabled so the document service signs the editor
  # config and OnlyOffice refuses unsigned load requests.
  # 4 GB RAM + 2 vCPU is the vendor-minimum; tune in production.
  onlyoffice:
    image: onlyoffice/documentserver:8.0
    container_name: vaultdms-onlyoffice
    environment:
      JWT_ENABLED: "true"
      JWT_SECRET: "${VAULTDMS_ONLYOFFICE_JWT:-dev-only-onlyoffice-jwt-rotate-in-prod}"
      JWT_HEADER: "Authorization"
      JWT_IN_BODY: "true"
    ports: ["8195:80"]
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost/healthcheck"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 60s
    restart: unless-stopped

  # §3.1 / B1 — Kong API gateway. DBless mode (declarative), no DB
  # dependency. Single source of public traffic; B2 will move every
  # public REST route here and strip `ports:` off the backends so they
  # are only reachable via the gateway. This B1 spike leaves backend
  # ports in place so existing dev tooling keeps working while the
  # route migration lands one PR at a time.
  gateway:
    image: kong:3.5-ubuntu
    container_name: vaultdms-gateway
    environment:
      KONG_DATABASE: "off"
      KONG_DECLARATIVE_CONFIG: /kong/declarative/kong.yaml
      KONG_PROXY_ACCESS_LOG: /dev/stdout
      KONG_ADMIN_ACCESS_LOG: /dev/stdout
      KONG_PROXY_ERROR_LOG: /dev/stderr
      KONG_ADMIN_ERROR_LOG: /dev/stderr
      # Admin API bound to 127.0.0.1 only — never reachable from the
      # network in dev. B2 disables it entirely in prod.
      KONG_ADMIN_LISTEN: "127.0.0.1:8001"
      # Status listener (health + /metrics) on 8100.
      KONG_STATUS_LISTEN: "0.0.0.0:8100"
      # Proxy listener — the single published port.
      KONG_PROXY_LISTEN: "0.0.0.0:8080"
      # §3.1 / B2.2 — shared secret injected into every forwarded
      # request as X-Gateway-Signature. kong.yaml references it as
      # `${VAULTDMS_GATEWAY_SECRET}`; Kong env-substitutes at boot.
      VAULTDMS_GATEWAY_SECRET: "${VAULTDMS_GATEWAY_SECRET:-dev-only-gateway-secret-rotate-in-prod}"
    volumes:
      - ./deploy/gateway:/kong/declarative:ro
    ports:
      - "8080:8080"   # public proxy
      - "8100:8100"   # /metrics + /status (prometheus)
    healthcheck:
      test: ["CMD", "kong", "health"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    restart: unless-stopped

  qdrant:
    image: qdrant/qdrant:v1.7.4
    container_name: vaultdms-qdrant
    ports: ["6333:6333", "6334:6334"]
    volumes:
      - qdrantdata:/qdrant/storage
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'exec 3<>/dev/tcp/localhost/6333'"]
      interval: 10s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  temporal:
    image: temporalio/auto-setup:1.22
    container_name: vaultdms-temporal
    environment:
      - DB=postgres12
      - DB_PORT=5432
      - POSTGRES_USER=vaultdms
      - POSTGRES_PWD=devpassword
      - POSTGRES_SEEDS=postgres
    ports: ["7233:7233"]
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "tctl", "--address", "temporal:7233", "cluster", "health"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 60s
    restart: unless-stopped

  temporal-ui:
    image: temporalio/ui:2.22.3
    container_name: vaultdms-temporal-ui
    environment:
      - TEMPORAL_ADDRESS=temporal:7233
      - TEMPORAL_CORS_ORIGINS=http://localhost:3000
    ports: ["8233:8080"]
    depends_on: [temporal]
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 15s
    restart: unless-stopped

  clamav:
    image: clamav/clamav:stable
    container_name: vaultdms-clamav
    ports: ["3310:3310"]
    volumes:
      - clamdata:/var/lib/clamav
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'exec 3<>/dev/tcp/localhost/3310'"]
      interval: 15s
      timeout: 3s
      retries: 20
      start_period: 120s
    restart: unless-stopped

  # ---------------------------------------------------------------------
  # Non-Go application services built from per-service Dockerfiles.
  # Started by default per §3.2 / A2 so `docker compose up` brings the
  # full compose tier up. The 11 Go services continue to run on the
  # host via `make run-all` (see scripts/run-all-services.sh); the
  # Sub-task B follow-up (§3.2 / A2 part 2) will migrate those into
  # compose as well.
  # ---------------------------------------------------------------------

  collaboration:
    build:
      # Dockerfile does COPY src/ / COPY package.json relative to the
      # service dir, not the repo root — context must match.
      context: services/collaboration
      dockerfile: Dockerfile
    container_name: vaultdms-collaboration
    environment:
      WS_PORT: "8083"
      REDIS_URL: "redis://redis:6379"
      AUTH_SERVICE_URL: "http://auth:8080"
      # §17.3 / D10 WS fan-out — annotation events bridged from NATS.
      VAULTDMS_NATS_URL: "nats://nats:4222"
    ports: ["8083:8083"]
    depends_on:
      redis: { condition: service_healthy }
    # Pure WebSocket server — no HTTP /healthz route. TCP probe on the
    # WS listener is the only app-code-free option and is sufficient to
    # detect "process up and accepting connections".
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "8083"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  # §7.1 / D6 part 2 — intelligence HTTP API. Serves /healthz, /ask,
  # /summarize, and /internal/v1/embed-query (the last is what the
  # search service calls for hybrid mode). Shares the Dockerfile with
  # intelligence-worker but uses the default uvicorn CMD.
  intelligence:
    build:
      context: services/intelligence
      dockerfile: Dockerfile
    container_name: vaultdms-intelligence
    environment:
      VAULTDMS_DATABASE_URL: "postgres://vaultdms:devpassword@postgres:5432/vaultdms?sslmode=disable"
      VAULTDMS_NATS_URL: "nats://nats:4222"
      VAULTDMS_QDRANT_URL: "http://qdrant:6333"
      VAULTDMS_S3_ENDPOINT: "minio:9000"
    ports: ["8194:8080"]
    depends_on:
      redis:    { condition: service_healthy }
      postgres: { condition: service_healthy }
      qdrant:   { condition: service_healthy }
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:8080/healthz',timeout=2).status==200 else 1)"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s
    restart: unless-stopped

  intelligence-worker:
    build:
      context: services/intelligence
      dockerfile: Dockerfile
    container_name: vaultdms-intelligence-worker
    command:
      - celery
      - "-A"
      - app.worker
      - worker
      - "--loglevel=info"
      - "-Q"
      - "intelligence,intelligence-ocr,intelligence-embed,intelligence-rag"
    environment:
      VAULTDMS_CELERY_BROKER_URL: "redis://redis:6379/1"
      VAULTDMS_DATABASE_URL: "postgres://vaultdms:devpassword@postgres:5432/vaultdms?sslmode=disable"
      VAULTDMS_NATS_URL: "nats://nats:4222"
      VAULTDMS_QDRANT_URL: "http://qdrant:6333"
      VAULTDMS_S3_ENDPOINT: "minio:9000"
    depends_on:
      redis:    { condition: service_healthy }
      postgres: { condition: service_healthy }
    # Celery worker: no HTTP surface. `inspect ping` round-trips the
    # broker so a healthy status means (worker process up) AND (worker
    # can still talk to Redis) — stronger than a port probe.
    healthcheck:
      test: ["CMD-SHELL", "celery -A app.worker inspect ping -t 5 | grep -q pong"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 60s
    restart: unless-stopped

  preview-worker:
    build:
      context: services/preview
      dockerfile: Dockerfile
    container_name: vaultdms-preview-worker
    command:
      - celery
      - "-A"
      - app.worker
      - worker
      - "--loglevel=info"
      - "-Q"
      - "preview,preview-video"
    environment:
      VAULTDMS_CELERY_BROKER_URL: "redis://redis:6379/2"
      VAULTDMS_S3_ENDPOINT: "minio:9000"
    depends_on:
      redis: { condition: service_healthy }
    healthcheck:
      test: ["CMD-SHELL", "celery -A app.worker inspect ping -t 5 | grep -q pong"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 60s
    restart: unless-stopped

  # ---------------------------------------------------------------------
  # Go services — §3.2 / A2 Sub-task B.
  # All 11 share one builder (build/Dockerfile.go-service) parameterized
  # by SERVICE. YAML anchors factor the common env/depends_on/healthcheck
  # so per-service blocks carry only what actually differs: SERVICE arg,
  # container_name, and host port mapping for the health port.
  # Container-internal ports are uniform (health=8081, http=8080,
  # grpc=9090); host ports follow scripts/run-all-services.sh so dev
  # tooling keeps working (but compose will fail to bind if `make
  # run-all` is already holding those ports — use one or the other).
  # ---------------------------------------------------------------------

  auth:
    build: &go-build-auth
      context: .
      dockerfile: build/Dockerfile.go-service
      args: { SERVICE: auth }
    container_name: vaultdms-auth
    environment: *go-env
    ports: ["8081:8081", "9090:9090", "8180:8080"]
    depends_on: *go-depends
    healthcheck: *go-healthcheck
    restart: unless-stopped

  policy:
    build:
      context: .
      dockerfile: build/Dockerfile.go-service
      args: { SERVICE: policy }
    container_name: vaultdms-policy
    environment: *go-env
    ports: ["8082:8081", "9091:9090", "8181:8080"]
    depends_on: *go-depends
    healthcheck: *go-healthcheck
    restart: unless-stopped

  document:
    build:
      context: .
      dockerfile: build/Dockerfile.go-service
      args: { SERVICE: document }
    container_name: vaultdms-document
    environment:
      <<: *go-env
      # Smoke-test override for ADR 0036/0037 internal cron endpoints
      # (retention sweep, disposition execute, DSR SLA sweep). Real
      # deploy injects via secret manager; dev compose treats it as
      # any other shared dev key.
      VAULTDMS_INTERNAL_API_KEY: dev-internal-api-key
    # Host health port 8084 instead of run-all-services.sh's 8083 to
    # avoid collision with the collaboration WS listener on 8083.
    ports: ["8084:8081", "9092:9090", "8182:8080"]
    depends_on: *go-depends
    healthcheck: *go-healthcheck
    restart: unless-stopped

  storage:
    build:
      context: .
      dockerfile: build/Dockerfile.go-service
      args: { SERVICE: storage }
    container_name: vaultdms-storage
    environment: *go-env
    ports: ["8085:8081", "9093:9090", "8183:8080"]
    depends_on: *go-depends
    healthcheck: *go-healthcheck
    restart: unless-stopped

  search:
    build:
      context: .
      dockerfile: build/Dockerfile.go-service
      args: { SERVICE: search }
    container_name: vaultdms-search
    environment: *go-env
    ports: ["8086:8081", "9094:9090", "8184:8080"]
    depends_on:
      postgres:   { condition: service_healthy }
      redis:      { condition: service_healthy }
      nats:       { condition: service_healthy }
      opensearch: { condition: service_healthy }
    healthcheck: *go-healthcheck
    restart: unless-stopped

  audit:
    build:
      context: .
      dockerfile: build/Dockerfile.go-service
      args: { SERVICE: audit }
    container_name: vaultdms-audit
    environment: *go-env
    ports: ["8087:8081", "9095:9090", "8185:8080"]
    depends_on: *go-depends
    healthcheck: *go-healthcheck
    restart: unless-stopped

  workflow:
    build:
      context: .
      dockerfile: build/Dockerfile.go-service
      args: { SERVICE: workflow }
    container_name: vaultdms-workflow
    environment: *go-env
    ports: ["8088:8081", "9096:9090", "8186:8080"]
    depends_on:
      postgres: { condition: service_healthy }
      redis:    { condition: service_healthy }
      nats:     { condition: service_healthy }
      temporal: { condition: service_healthy }
    healthcheck: *go-healthcheck
    restart: unless-stopped

  notification:
    build:
      context: .
      dockerfile: build/Dockerfile.go-service
      args: { SERVICE: notification }
    container_name: vaultdms-notification
    environment: *go-env
    ports: ["8089:8081", "9097:9090", "8187:8080"]
    depends_on: *go-depends
    healthcheck: *go-healthcheck
    restart: unless-stopped

  signature:
    build:
      context: .
      dockerfile: build/Dockerfile.go-service
      args: { SERVICE: signature }
    container_name: vaultdms-signature
    environment: *go-env
    ports: ["8090:8081", "9098:9090", "8188:8080"]
    depends_on: *go-depends
    healthcheck: *go-healthcheck
    restart: unless-stopped

  billing:
    build:
      context: .
      dockerfile: build/Dockerfile.go-service
      args: { SERVICE: billing }
    container_name: vaultdms-billing
    environment: *go-env
    ports: ["8091:8081", "9099:9090", "8189:8080"]
    depends_on: *go-depends
    healthcheck: *go-healthcheck
    restart: unless-stopped

  connector:
    build:
      context: .
      dockerfile: build/Dockerfile.go-service
      args: { SERVICE: connector }
    container_name: vaultdms-connector
    environment: *go-env
    ports: ["8092:8081", "9100:9090", "8190:8080"]
    depends_on: *go-depends
    healthcheck: *go-healthcheck
    restart: unless-stopped

volumes:
  pgdata:
  redisdata:
  opensearchdata:
  miniodata:
  natsdata:
  qdrantdata:
  clamdata:
  prometheusdata:
