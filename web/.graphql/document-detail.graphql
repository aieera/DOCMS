query DocumentDetail($id: ID!) { document(id: $id) { id tenantId workspaceId folderId title description lifecycleState regionPin documentClass classificationConfidence tags mimeType totalSizeBytes sha256Hash createdBy createdAt updatedAt currentVersion { id versionNumber sizeBytes mimeType sha256Hash createdAt createdByName changeSummary } versions(limit: 20) { nodes { id versionNumber sizeBytes createdAt createdByName changeSummary } } comments { nodes { id body authorName authorId createdAt resolved replies { id body authorName createdAt } } } annotations { nodes { id kind page createdAt } } workflowInstances { id status definitionName startedAt tasks { id title status assigneeName dueAt } } permissions { canView canEdit canDelete canShare canAdmin } } }
