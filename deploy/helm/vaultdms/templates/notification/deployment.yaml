{{- if .Values.notification.enabled }}
{{- include "vaultdms.goServiceDeployment" (dict "svcName" "notification" "svc" .Values.notification "Values" .Values "Release" .Release "Chart" .Chart) }}
{{- end }}
